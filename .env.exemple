DB_HOST=gymtrack-db        # Nom du service Docker (voir docker-compose.yml)
DB_USER=root               # Utilisateur MySQL
DB_PASSWORD=               # Mot de passe MySQL — il existe que dans le fichier .env
DB_NAME=gymtrack           # Nom de la base de données
